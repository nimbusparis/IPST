<Window x:Class="IPST_GUI.View.ChartView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:ignore="http://www.ignore.com"
        mc:Ignorable="d ignore"
        Title="Charts"
        DataContext="{Binding Charts, Source={StaticResource Locator}}">
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="1*" />
        </Grid.RowDefinitions>
        <Menu>
            <MenuItem Header="Pending portals" Command="{Binding SubmissionChartCommand}" CommandParameter="Pending"/>
            <MenuItem Header="Accepted Portals" Command="{Binding SubmissionChartCommand}" CommandParameter="Accepted"/>
            <MenuItem Header="Rejected portals" Command="{Binding SubmissionChartCommand}" CommandParameter="Rejected"/>
        </Menu>
        
   </Grid>
</Window>
